"""
整型
进制转换
10-2
除二取余
bin()
2-10
按权展开求和
int("1010",2)
print(0b1010)

字符串
不可变数据类型
索引，切片
.upper()全部大写
.lower()全部小写
.startswith() 判断是否以xx开头，支持切片
.endswith() 判断是否以xx结尾，支持切片
.count() 统计xx的数量
.replace() 旧值，新值，替换次数（默认全部）
.split()分割为列表，默认空格，\n,\t，可指定内容分割，可指定分割次数
.strip() 删除头尾所有的空格，\n,\t，可指定内容删除头尾
.isalnum 判断是否由中文，字母，数字组成
.isalpha 判断是否由中文，字母组成
.isdigit 判断是否由阿拉伯数字组成（①--bug）
.isdecimal 判断是否为十进制数

for循环
for i in xxxx:

xxxx-可迭代对象
"""